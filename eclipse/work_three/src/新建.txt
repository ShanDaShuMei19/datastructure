本项目的设计要求可以分为三个部分实现：其一，建
立一个文本文件，文件名由用户用键盘输入；其二，
给定单词计数，输入一个不含空格的单词，统计输出
该单词在文本中的出现次数；其三，检索给定单词，
输入一个单词，检索并输出该单词所在的行号、该行
中出现的次数以及在该行中的相应位置。
1. 建立文件的实现思路是：
(1) 定义一个串变量；
(2) 定义文本文件；
(3) 输入文件名，打开该文件；
(4) 循环读入文本行，写入文本文件，其过程如下：
	while(不是文件输入结束){	
		读入一文本行至串变量；
		串变量写入文件；
		输入是否结束输入标志；
	｝
(5) 关闭文件。

